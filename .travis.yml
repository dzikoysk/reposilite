language: java
sudo: true

jdk:
- openjdk8

script:
  - sudo mvn test jacoco:report-aggregate

after_success:
  - sudo bash <(curl -s https://codecov.io/bash)
  - sudo mvn coveralls:report

notifications:
  email: false
