apiVersion: v1
kind: Service
metadata:
  name: {{ include "reposilite-helm.fullname" . }}
  labels:
    {{- include "reposilite-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reposilite-helm.selectorLabels" . | nindent 4 }}
